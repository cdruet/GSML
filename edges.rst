Edges simply define the relationships among the nodes. The only constraint when defining an edge is to link an output variable of one node to an input variable of another node, or the other way around. Note that internal variables cannot be linked.

.. literalinclude:: ./examples/edges.txt
   :language: text
   :emphasize-lines: 6,16,25

